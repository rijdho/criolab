{
  "name": "criolab",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Tests de las líneas de CrioLab (node --test, sin dependencias).",
  "scripts": {
    "test": "node --test"
  }
}
